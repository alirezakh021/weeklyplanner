{
  "name": "weekly-planner",
  "version": "1.0.0",
  "private": true,
  "description": "Personal weekly planner (Persian, fixed + variable day tasks) - self-hosted",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
